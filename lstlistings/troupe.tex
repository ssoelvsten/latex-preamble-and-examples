% from: modification of `sml.tex` for Troupe

\lstdefinelanguage{troupe}{
  keywords=[1]{
    and, andalso, array, case, datatype, else, end, false, fn, fun, hn, if, in,
    include, let, not, of, orelse, print, raisedTo, receive, sandbox, send,
    spawn, then, true, val, where
  },
  sensitive=false,
  morecomment=[s]{(*}{*)},
  morestring=[b]"
}
